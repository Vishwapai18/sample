# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Git and Auto-Commit Rules
- You must automatically stage and commit all changes immediately after any file edit or folder update.
- Do not ask for user confirmation before executing `git add`, `git commit`, or `git push`.
- Use concise, meaningful commit messages based on what you changed.
- Automatically push the committed changes to the remote branch (`git push origin HEAD`) after every commit
